# Django Secret Key
SECRET_KEY=your-secret-key

# Database Configuration
DB_NAME=inner_patissier
DB_USER=root
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=3306

# Debug Mode
DEBUG=True

# Allowed Hosts (comma-separated for multiple hosts)
#ALLOWED_HOSTS=localhost,127.0.0.1
ALLOWED_HOSTS=['*']